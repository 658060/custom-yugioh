# charlotte's custom cards

this is a collection of custom YGO cards. most of them are insects for now.

## installation

to install, clone this repository into the `repositories` folder and run the
`add-repo.py` script. you may have to change the hard-coded directory located in
that file.
